import { sum } from '../src';
//import { mocha } from "@types/mocha";
//import  { chai } from "mocha"
import { expect } from "chai"; 
//import { describe } from "@"
//import {j}
describe('blah', () => {
  it('works', () => {
    sum(1, 1);//).toEqual(2);
  });
});
